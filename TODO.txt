
Creer la fonction free pour l'arbre binaire (structure tree) et ajouter la structure tree dans la fonction struct_parssing;
dans la fonction pre parsse, ajouter un espace avant et apres un separateur si il n'y en a pas.

Dans le remplissage de l'arbre binaire les variables *cmd comportes un espace ne trop en fin de ligne.
Tester le parssing avec tout les separateurs restant (>>, <<, <). Le reste fonctionne...


Avant l'execution de la commande dans l'arbre binaire, il nous faudra utiliser la fonction de parsing de ligne, qui nous servira a transformer toute les demandes de variable en valeur de celle-ci.
Attention si dans le parssing nous rencontrons quelquechose=untruc il nous faut declarer cette variable dans la memoire et non dans l'environnement, hormis dans me cas ou la command de gauche (tree) est export.

wildcard a effectuer ajouter le '?' ?? outsanding project.. :
	loundor@Surface:~$ ls p***********c
		pwd.c
	loundor@Surface:~$ ls p*******d*******c
		pwd.c
	loundor@Surface:~$ ls p*******d*******c
		pwd.c
	loundor@Surface:~$ ls ******p*******d*******c
		pwd.c
	loundor@Surface:~$ ls ******p*******d*******c********
		pwd.c
	loundor@Surface:~$ ls p?*
		pwd.c
	loundor@Surface:~$ ls p?*c
		pwd.c
	loundor@Surface:~$ ls p?d.c
		pwd.c

LES PARAMETRES DE EXECVE EST UN TABLEAU!!!!
Probleme double espace dans le parssing de la cmd avec les simple et double quotes. (Non fixé??!! A tester dans un premier temps.)
Dans exec.c ajouter un arret dans le while pour || | && << >> < >... // DEPRECATED NE PAS FAIRE LA FONCTION SERA PEUT ETRE SUPPRIMER CAR UTILISATION DE LA STRUCT TREE

fichier line.c plus de 5 fonctions...
Fichiers double quotes et single quotes plus de 25 lignes!!!